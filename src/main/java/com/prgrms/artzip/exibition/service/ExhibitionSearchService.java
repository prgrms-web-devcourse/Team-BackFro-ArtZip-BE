package com.prgrms.artzip.exibition.service;

public class ExhibitionSearchService {
  // 헤더 검색
  // query, area, month, pageable
  // 입력 받은 경우 해당 값들 검증
  // query : 필수로 입력
  // area : null이 아닌 경우
  // moth : null이 아닌 경우 1 ~ 12

  // 후기 작성시 검색
  // query, pageable
  // query : 필수로 입력 (notblank)
}
